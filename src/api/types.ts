export type TFile = {
  id: string;
  url: string;
  width: number;
  height: number;
  alternativeText: string;
};

export type TMenu = {
  id: string;
  title: string;
  url: string;
};

export type TComponentButtonButton = {
  id: string;
  text: string;
  url: string;
};

export type TComponentTagTag = {
  id: string;
  title: string;
};
